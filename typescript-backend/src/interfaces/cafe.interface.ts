export interface ICafe {
  id: number;
  name: string;
  address: string;
  openingDays: string;
  googleRating: number;
  startPrice: number;
  endPrice: number;
}

// Add another like final calculated rating appended to